<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Thank you for placing an order at jungle.com!</h1>
    <h2>Your order details are below.</h2>
    
    <h2>Order Total: <%= humanized_money_with_symbol @order.total_cents / 100.00 %></h2>

    <table>
      <tr>
        <th>Item</th>
      </tr>
      <% @cart.each do |item| %>
        <tr>
          <td><%= item[:product].name %></td>
        </tr>
      <% end %>
    </table>

  </body>
</html>

